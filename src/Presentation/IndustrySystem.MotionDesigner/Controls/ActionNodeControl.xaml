<UserControl x:Class="IndustrySystem.MotionDesigner.Controls.ActionNodeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="{Binding Width}" Height="{Binding Height}">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Port Style -->
        <Style x:Key="PortStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="Fill" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Setter Property="Stroke" Value="White"/>
            <Setter Property="StrokeThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Width" Value="14"/>
                    <Setter Property="Height" Value="14"/>
                    <Setter Property="Fill" Value="#4CAF50"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="NodeBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    <Setter Property="BorderThickness" Value="3"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#3F51B5" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsExecuting}" Value="True">
                    <Setter Property="BorderBrush" Value="#4CAF50"/>
                    <Setter Property="BorderThickness" Value="3"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding HasError}" Value="True">
                    <Setter Property="BorderBrush" Value="#F44336"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <!-- Top Port -->
        <Ellipse Style="{StaticResource PortStyle}"
                HorizontalAlignment="Center" VerticalAlignment="Top"
                Margin="0,-6,0,0" x:Name="TopPort"
                MouseLeftButtonDown="OnPortMouseDown"
                ToolTip="Top Port - Click to connect"/>
        
        <!-- Left Port -->
        <Ellipse Style="{StaticResource PortStyle}"
                HorizontalAlignment="Left" VerticalAlignment="Center"
                Margin="-6,0,0,0" x:Name="LeftPort"
                MouseLeftButtonDown="OnPortMouseDown"
                ToolTip="Left Port - Click to connect"/>
        
        <!-- Bottom Port -->
        <Ellipse Style="{StaticResource PortStyle}"
                HorizontalAlignment="Center" VerticalAlignment="Bottom"
                Margin="0,0,0,-6" x:Name="BottomPort"
                MouseLeftButtonDown="OnPortMouseDown"
                ToolTip="Bottom Port - Click to connect"/>
        
        <!-- Right Port -->
        <Ellipse Style="{StaticResource PortStyle}"
                HorizontalAlignment="Right" VerticalAlignment="Center"
                Margin="0,0,-6,0" x:Name="RightPort"
                MouseLeftButtonDown="OnPortMouseDown"
                ToolTip="Right Port - Click to connect"/>
        
        <Border Style="{StaticResource NodeBorder}" Background="White" Margin="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" Background="{Binding HeaderColor}" CornerRadius="6,6,0,0" Padding="8,4">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="{Binding IconKind}" Width="18" Height="18"
                                                Foreground="White" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="SemiBold"
                                  FontSize="12" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Row="1" Background="White" CornerRadius="0,0,6,6" Padding="8,4">
                    <TextBlock Text="{Binding Description}" FontSize="10" Opacity="0.7"
                              TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="30"/>
                </Border>
            </Grid>
        </Border>
        
        <!-- Executing Indicator -->
        <Border HorizontalAlignment="Right" VerticalAlignment="Top"
               Margin="0,-4,-4,0" Width="16" Height="16" CornerRadius="8" Background="#4CAF50"
               Visibility="{Binding IsExecuting, Converter={StaticResource BooleanToVisibilityConverter}}">
            <materialDesign:PackIcon Kind="Play" Width="10" Height="10" Foreground="White"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
