 
<UserControl x:Class="IndustrySystem.Presentation.Wpf.Views.RealtimeDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:oxy="http://oxyplot.org/wpf"
             prism:ViewModelLocator.AutoWireViewModel="False"
             Background="{DynamicResource SystemControlPageBackgroundAltHighBrush}">
  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <Grid Margin="20">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="12"/>
        <RowDefinition Height="280"/>
        <RowDefinition Height="12"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <!-- Header -->
      <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock Text="&#xE9F9;" FontFamily="Segoe MDL2 Assets" FontSize="26"
                   Foreground="{DynamicResource SystemControlForegroundAccentBrush}"
                   VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBlock Text="实时数据与趋势" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
        <TextBlock VerticalAlignment="Center" Margin="12,0,0,0" FontSize="13"
                   Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}">
          <Run Text="（当前监控: "/>
          <Run Text="{Binding CurrentMonitor,Mode=OneWay}"/>
          <Run Text="）"/>
        </TextBlock>
      </StackPanel>

      <!-- 温度趋势图卡片 -->
      <Border Grid.Row="2"
              Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
              CornerRadius="8" BorderThickness="1"
              BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="16,12,16,0">
            <TextBlock Text="&#xE9F3;" FontFamily="Segoe MDL2 Assets" FontSize="15"
                       Foreground="#FFE85289" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <TextBlock Text="温度趋势曲线" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBlock VerticalAlignment="Center" Margin="12,0,0,0" FontSize="12"
                       Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}">
              <Run Text="（当前监控: "/>
              <Run Text="{Binding CurrentMonitor,Mode=OneWay}"/>
              <Run Text="）"/>
            </TextBlock>
          </StackPanel>
          <oxy:PlotView Grid.Row="1" Margin="8,4,8,8"
                        Model="{Binding TrendModel}"
                        Background="Transparent"/>
        </Grid>
      </Border>

      <!-- 传感器指标卡片网格 -->
      <ItemsControl Grid.Row="4" ItemsSource="{Binding Sensors}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <UniformGrid Columns="4" IsItemsHost="True"/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Margin="0,0,12,12"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="8" BorderThickness="1"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                    Padding="16,14">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="8"/>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="6"/>
                  <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="{Binding Label}" FontSize="13"
                           Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Bottom">
                  <TextBlock Text="{Binding ValueText}" FontSize="28" FontWeight="Bold"
                             VerticalAlignment="Bottom"/>
                  <TextBlock Text="{Binding UnitSmall}" FontSize="13" Margin="4,0,0,4"
                             VerticalAlignment="Bottom"
                             Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                </StackPanel>
                <oxy:PlotView Grid.Row="4"
                              Model="{Binding MiniPlotModel}"
                              Background="Transparent"/>
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </Grid>
  </ScrollViewer>
</UserControl>
