<UserControl x:Class="IndustrySystem.Presentation.Wpf.Views.MotionProgramRunView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:IndustrySystem.Presentation.Wpf.Controls"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MaterialDesignPaper}">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <materialDesign:PackIcon Kind="RobotIndustrial" Width="32" Height="32" 
                                    VerticalAlignment="Center" Foreground="{StaticResource PrimaryHueMidBrush}"/>
            <TextBlock Text="Motion Program Executor" FontSize="24" FontWeight="Light"
                      VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>
        
        <materialDesign:Card Grid.Row="1" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="8">
            <controls:MotionProgramViewer DataContext="{Binding}"/>
        </materialDesign:Card>
    </Grid>
</UserControl>
