<UserControl x:Class="IndustrySystem.Presentation.Wpf.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal">
      <TextBox x:Name="UserNameBox" Width="200" Margin="0,0,8,0"/>
      <TextBox x:Name="DisplayNameBox" Width="200" Margin="0,0,8,0"/>
      <Button Content="新增用户" Click="OnAdd"/>
    </StackPanel>
    <ListView Grid.Row="1" ItemsSource="{Binding Users}">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="用户名" DisplayMemberBinding="{Binding UserName}"/>
          <GridViewColumn Header="显示名" DisplayMemberBinding="{Binding DisplayName}"/>
          <GridViewColumn Header="启用" DisplayMemberBinding="{Binding IsActive}"/>
          <GridViewColumn Header="操作">
            <GridViewColumn.CellTemplate>
              <DataTemplate>
                <Button Content="删除" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding Id}"/>
              </DataTemplate>
            </GridViewColumn.CellTemplate>
          </GridViewColumn>
        </GridView>
      </ListView.View>
    </ListView>
  </Grid>
</UserControl>
 
