<UserControl x:Class="IndustrySystem.Presentation.Wpf.Views.LoginView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:loc="clr-namespace:IndustrySystem.Presentation.Wpf.Resources"
 xmlns:conv="clr-namespace:IndustrySystem.Presentation.Wpf.Converters"
 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
	<UserControl.Resources>
		<loc:LocalizationProvider x:Key="Loc" />
		<Thickness x:Key="Gap">8</Thickness>
		<conv:BooleanNegationConverter x:Key="Not" />
	</UserControl.Resources>
	<Grid Margin="24">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- Header -->
		<StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,12">
			<Image Width="40" Height="40" Source="/IndustrySystem.Presentation.Wpf;component/Assets/logo.png" Margin="0,0,10,0"/>
			<TextBlock Text="{Binding [Page_Login_Title], Source={StaticResource Loc}}" FontSize="20" FontWeight="SemiBold"/>
		</StackPanel>

		<!-- Card -->
		<Border Grid.Row="1" Padding="20" CornerRadius="12" Background="#0F000000" BorderBrush="#20000000" BorderThickness="1"
				HorizontalAlignment="Center" VerticalAlignment="Top">
			<Border.Style>
				<Style TargetType="Border">
					<Style.Triggers>
						<DataTrigger Binding="{Binding HasError}" Value="True">
							<Setter Property="BorderBrush" Value="Crimson"/>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Border.Style>
			<StackPanel Width="360">
				<TextBlock Text="{Binding [Field_User_UserName], Source={StaticResource Loc}}"/>
				<TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
						 Margin="{StaticResource Gap}"
						 Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"/>
				<TextBlock Margin="0,8,0,0" Text="{Binding [Field_User_Password], Source={StaticResource Loc}}"/>
				<PasswordBox x:Name="Pwd" Style="{StaticResource MaterialDesignOutlinedPasswordBox}" Margin="{StaticResource Gap}"/>
				<TextBlock Margin="0,4,0,0" Foreground="Crimson" Text="{Binding ErrorMessage}"/>
				<StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,8,0,0">
					<CheckBox Content="{Binding [Login_Remember], Source={StaticResource Loc}}" IsChecked="{Binding Remember}"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" Margin="0,12,0,0" HorizontalAlignment="Right">
					<Button Content="{Binding [Login_Quick], Source={StaticResource Loc}}" Command="{Binding QuickLoginCommand}" Margin="12,0,0,0"/>
					<Button Content="{Binding [Btn_Cancel], Source={StaticResource Loc}}" Margin="8,0,8,0" Command="{Binding CancelCommand}"/>
					<Button Content="{Binding [Btn_Login], Source={StaticResource Loc}}" Command="{Binding LoginCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource Not}}"/>
				</StackPanel>
			</StackPanel>
		</Border>
	</Grid>
</UserControl>
