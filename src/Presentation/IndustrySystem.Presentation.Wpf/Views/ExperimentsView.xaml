<UserControl x:Class="IndustrySystem.Presentation.Wpf.Views.ExperimentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:IndustrySystem.Presentation.Wpf.Resources"
             Background="{DynamicResource SystemControlPageBackgroundAltHighBrush}">
  <Grid Style="{StaticResource PageRootGrid}">
    <Border Style="{StaticResource FormCard}" VerticalAlignment="Top">
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock Text="&#xE8F1;" FontFamily="Segoe MDL2 Assets" FontSize="24"
                   Foreground="{DynamicResource SystemControlForegroundAccentBrush}" Margin="0,0,10,0"/>
        <TextBlock Text="{x:Static loc:Strings.Page_ExperimentsPlaceholder_Title}" Style="{StaticResource PageHeaderTitle}" />
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
