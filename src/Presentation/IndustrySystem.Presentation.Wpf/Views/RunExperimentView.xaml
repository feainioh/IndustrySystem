<UserControl x:Class="IndustrySystem.Presentation.Wpf.Views.RunExperimentView"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal">
      <TextBlock Text="当前实验：" VerticalAlignment="Center"/>
      <ComboBox Width="220" Margin="12,0,0,0" ItemsSource="{Binding Experiments}" SelectedItem="{Binding SelectedExperiment}" DisplayMemberPath="Name"/>
      <TextBlock Text="(当前：)" VerticalAlignment="Center" Margin="12,0,0,0"/>
      <TextBlock Text="{Binding CurrentExperiment}" FontWeight="Bold" VerticalAlignment="Center"/>
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
      <TextBlock Text="状态：" VerticalAlignment="Center"/>
      <TextBlock Text="{Binding RunStatus}" FontWeight="Bold" VerticalAlignment="Center" Margin="12,0,0,0"/>
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0" VerticalAlignment="Center">
      <TextBlock Text="进度：" VerticalAlignment="Center"/>
      <ProgressBar Width="300" Height="14" Minimum="0" Maximum="100" Value="{Binding Progress}" Margin="12,0,0,0"/>
      <TextBlock Text="{Binding Progress, StringFormat={}{0}%}" VerticalAlignment="Center" Margin="12,0,0,0"/>
    </StackPanel>

    <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,16,0,0">
      <StackPanel Orientation="Horizontal">
        <Button Content="开始" Command="{Binding StartCommand}" Width="80" IsEnabled="{Binding CanStart}"/>
        <Button Content="暂停" Command="{Binding PauseCommand}" Width="80" Margin="12,0,0,0" IsEnabled="{Binding CanPause}"/>
        <Button Content="继续" Command="{Binding ResumeCommand}" Width="80" Margin="12,0,0,0" IsEnabled="{Binding CanResume}"/>
        <Button Content="停止" Command="{Binding StopCommand}" Width="80" Margin="12,0,0,0" IsEnabled="{Binding CanStop}"/>
      </StackPanel>
      <TextBlock Margin="0,12,0,0" Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="DarkSlateGray"/>
    </StackPanel>
  </Grid>
</UserControl>
