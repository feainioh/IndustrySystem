<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="IndustrySystem.Presentation.Wpf.Views.Dialogs.PermissionEditDialog"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:prism="http://prismlibrary.com/"
 xmlns:loc="clr-namespace:IndustrySystem.Presentation.Wpf.Resources"
 prism:ViewModelLocator.AutoWireViewModel="False">
 <UserControl.Resources>
 <loc:LocalizationProvider x:Key="Loc" />
 </UserControl.Resources>
 <Grid Margin="16">
 <Grid.RowDefinitions>
 <RowDefinition Height="Auto"/>
 <RowDefinition Height="*"/>
 <RowDefinition Height="Auto"/>
 </Grid.RowDefinitions>
 <TextBlock Text="{Binding [Dialog_PermissionEdit_Title], Source={StaticResource Loc}}" FontSize="18" FontWeight="SemiBold"/>
 <StackPanel Grid.Row="1" Margin="0,12,0,12">
 <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
 <TextBlock Text="{Binding [Field_Common_Name], Source={StaticResource Loc}}" Width="120" VerticalAlignment="Center"/>
 <TextBox Width="240"
 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True, NotifyOnValidationError=True}"/>
 </StackPanel>
 <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
 <TextBlock Text="{Binding [Field_Common_DisplayName], Source={StaticResource Loc}}" Width="120" VerticalAlignment="Center"/>
 <TextBox Width="300"
 Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True, NotifyOnValidationError=True}"/>
 </StackPanel>
 </StackPanel>
 <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
 <Button Content="{Binding [Btn_Cancel], Source={StaticResource Loc}}" Margin="0,0,8,0" Command="{Binding CancelCommand}"/>
 <Button Content="{Binding [Btn_Save], Source={StaticResource Loc}}" Command="{Binding SaveCommand}"/>
 </StackPanel>
 </Grid>
</UserControl>
