<UserControl x:Class="IndustrySystem.Presentation.Wpf.Views.ExperimentTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:loc="clr-namespace:IndustrySystem.Presentation.Wpf.Resources"
             prism:ViewModelLocator.AutoWireViewModel="False"
             Background="{DynamicResource SystemControlPageBackgroundAltHighBrush}">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="16"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="16"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Header -->
    <Grid Grid.Row="0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      
      <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock Text="&#xE8A5;" FontFamily="Segoe MDL2 Assets" FontSize="28" 
                   Foreground="{DynamicResource SystemControlForegroundAccentBrush}" 
                   VerticalAlignment="Center" Margin="0,0,12,0"/>
        <TextBlock Text="{x:Static loc:Strings.Nav_Templates}" 
                   FontSize="24" FontWeight="SemiBold"
                   VerticalAlignment="Center"/>
      </StackPanel>
      
      <Button Grid.Column="1" 
              Command="{Binding RefreshCommand}"
              Style="{DynamicResource AccentButtonStyle}"
              Padding="16,8">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" Margin="0,0,8,0" VerticalAlignment="Center"/>
          <TextBlock Text="{x:Static loc:Strings.Btn_Refresh}" VerticalAlignment="Center"/>
        </StackPanel>
      </Button>
    </Grid>

    <!-- Input Form Card -->
    <Border Grid.Row="2" 
            Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
            CornerRadius="8" Padding="16"
            BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
            BorderThickness="1">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="2*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Column="0" Text="{x:Static loc:Strings.Field_Common_Name}" 
                   VerticalAlignment="Center" Margin="0,0,12,0" FontWeight="Medium"/>
        <TextBox x:Name="NameBox" Grid.Column="1" 
                 ui:ControlHelper.PlaceholderText="{x:Static loc:Strings.Hint_Template_Name}"
                 VerticalAlignment="Center"/>
        
        <TextBlock Grid.Column="2" Text="{x:Static loc:Strings.Field_Common_Description}" 
                   VerticalAlignment="Center" Margin="24,0,12,0" FontWeight="Medium"/>
        <TextBox x:Name="DescBox" Grid.Column="3" 
                 ui:ControlHelper.PlaceholderText="{x:Static loc:Strings.Hint_Template_Description}"
                 VerticalAlignment="Center"/>
        
        <Button Grid.Column="4" Click="OnAdd" Margin="24,0,0,0"
                Style="{DynamicResource AccentButtonStyle}" Padding="16,8">
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <TextBlock Text="{x:Static loc:Strings.Btn_AddTemplate}" VerticalAlignment="Center"/>
          </StackPanel>
        </Button>
      </Grid>
    </Border>

    <!-- Templates List Card -->
    <Border Grid.Row="4" 
            Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
            CornerRadius="8"
            BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
            BorderThickness="1">
      <ListView ItemsSource="{Binding Templates}"
                Background="Transparent"
                BorderThickness="0"
                ui:ScrollViewerHelper.AutoHideScrollBars="True">
        <ListView.View>
          <GridView>
            <GridViewColumn Width="250">
              <GridViewColumn.Header>
                <TextBlock Text="{x:Static loc:Strings.Col_Common_Name}" FontWeight="SemiBold"/>
              </GridViewColumn.Header>
              <GridViewColumn.CellTemplate>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal">
                    <Border Width="32" Height="32" CornerRadius="16" 
                            Background="{DynamicResource SystemControlForegroundAccentBrush}" Margin="0,0,12,0">
                      <TextBlock Text="&#xE8A5;" FontFamily="Segoe MDL2 Assets" 
                                 Foreground="White" FontSize="14"
                                 HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="Medium"/>
                  </StackPanel>
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
            <GridViewColumn Width="350">
              <GridViewColumn.Header>
                <TextBlock Text="{x:Static loc:Strings.Col_Common_Description}" FontWeight="SemiBold"/>
              </GridViewColumn.Header>
              <GridViewColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Description}" VerticalAlignment="Center" 
                             TextTrimming="CharacterEllipsis"
                             Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
            <GridViewColumn Width="120">
              <GridViewColumn.Header>
                <TextBlock Text="{x:Static loc:Strings.Col_Common_Actions}" FontWeight="SemiBold"/>
              </GridViewColumn.Header>
              <GridViewColumn.CellTemplate>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal">
                    <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                            CommandParameter="{Binding Id}"
                            Background="Transparent" BorderThickness="0"
                            Padding="8,4" ToolTip="{x:Static loc:Strings.Btn_Delete}">
                      <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" 
                                 Foreground="#FFDC3545" FontSize="14"/>
                    </Button>
                  </StackPanel>
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
          </GridView>
        </ListView.View>
      </ListView>
    </Border>
  </Grid>
</UserControl>
